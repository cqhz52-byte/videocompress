{
  "name": "videocompress",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "npx vercel dev",
    "check": "node --check app.js && node --check service-worker.js && node --check api/translate.js"
  }
}
